import styled from 'styled-components';

const ContactBlockFormWrapper = styled.div`
	grid-column: span 6;
	color: var(--colour-white);
`;

const ContactBlockForm = () => {
	return (
		<ContactBlockFormWrapper>
			CONTACT FORM HERE
		</ContactBlockFormWrapper>
	);
};

export default ContactBlockForm;
